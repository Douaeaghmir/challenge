//Write a JavaScript function that takes two numbers as parameters and returns the larger of the two. 
// Use this function to display the maximum between two numbers.

function comp(a , b)
{
	if (a > b)
		return(a);
	else
		return(b);
}
console.log(comp(2 , 3));	
