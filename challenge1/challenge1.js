//Write a program that asks for a number and displays whether that number is even or odd.
const prompt = require("prompt-sync")();
const n1 = prompt("what the value a : ");

if(n1 % 2 == 0)
    console.log("even");
else
    console.log("odd")
const n2 = prompt("what the value b : ");
 if(n2 % 2 == 0)
    console.log("even");
else
    console.log("odd")
